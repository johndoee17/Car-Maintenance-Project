{
  "name": "car-maintenance-tracker",
  "private": true,
  "version": "1.0.0",
  "description": "Secure full-stack Car Maintenance Tracker using React, Express and MongoDB.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n API,WEB -c auto \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm run start --prefix backend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
